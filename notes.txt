import titan.resources
from titan.qt import QtCore, QtGui, QtWidgets

from titan._internal.preferences.main import Preferences, create_preferences_widget
from titan._internal.preferences.parser import load_preferences_from_file

preferences_file = titan.resources.find_resource("preferences.txt")
preferences = Preferences.from_file(preferences_file)

pref = load_preferences_from_file(preferences_file)
pref.recursive_print()

widget = create_preferences_widget(preferences)
widget.show()
